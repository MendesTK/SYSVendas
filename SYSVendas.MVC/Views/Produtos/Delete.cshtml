@model SYSVendas.MVC.ViewModels.ProdutoViewModel

@{
    ViewBag.Title = "Delete";
}
    <h6>Você tem certeza que deseja deletar o produto?</h6>
    <div>
        

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Sim" class="btn btn-danger" /> 
                @Html.ActionLink("Não", "Index", new { }, new { @class="btn btn-dark"})
            </div>
        }
    </div>
